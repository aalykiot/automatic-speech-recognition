#### Plotting signals ####

# plt_rows = len(words) + 1

# fig, axs = plt.subplots(plt_rows)

# axs[0].plot(signal)

# for index, signal in enumerate(words):
#     axs[index + 1].plot(signal)

# plt.tight_layout()
# fig.set_size_inches(18.5, 10.5, forward=True)
# fig.savefig("plots.png")
